<task>
现在需要确定data pipeline，并根据pipeline中各环节指定单元、集成、e2e测试。本项目系统分为学生咨询端mini-app和院校管理端admin-spa。
解决现有的error
当前开发目标为：在本地使用docker实现单所院校scnu的完整流程。
</task>

<Data pipeline>
是指当有数据进入该系统时各模块如何响应以及处理对应数据。
首先，学生打开学生端网页，使用注册或体验（提供双入口）进入主页面（AI咨询页），
对于使用注册进入的用户创建专属的session_id以实现持久化对话记录（保持一个月），游客则随机创建临时id，保持1天。
学生对话时，AI助手需要引导学生说出一些个人信息，捕捉到信息后需要将其存入后端该用户的数据库。使用JSON结构化存储。具体所需数据为“报考建议”页中考生信息所需字段，属于基本信息。以及其个人画像构建所需的信息（非结构化，由AI进行总结整理）。
考生基本信息会展示在报考建议页的考生信息处。个人画像及相关信息用于生成专业报考建议。
考生咨询完毕后，其咨询信息会被整理，出现在院校管理端平台。分别为：
1. 线索管理。具体根据现有示范条例总结出规范格式。
2. 咨询管理。具体根据现有示范条例总结出规范格式。
3. 渠道报告。具体根据现有示范条例总结出规范格式。
4. 工作台所有相关数据仪表盘的数据也要更新
</Data pipeline>

<error>
1. 洞察分析、画像看板板块现在仍然显示 "Request failed with status code 403" 报错，需要检查问题并修复。（高优先级）
2. 知识库页面显示索引数据为0，需检查并导入数据。可参考项目repo分支"feat/d-infrastructure"及文件"./docs\ROLE_D_RAG_INTEGRATION_REPORT.md"（高优先级）
3. mini-app AI咨询页面，当询问AI信息时，会出现“检索中”，“正在生成回答”及其类似提醒，但是其位置为对话页面的中间，而不是预留的对话气泡中，需要修复（低优先级）
</error>
